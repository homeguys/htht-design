```jsx
import BasePlottingInfo from '../webMap/entity/BasePlottingInfo'
```

---

```jsx
new BasePlottingInfo({
        id: info.id,
        name: info.name,
        position: [info.position.longitude, info.position.latitude],
        dataInfo: info.dataInfo
      })
```

---

```jsx
new BasePlottingInfo({
        id: info.id,
        name: info.name,
        position: [info.position.longitude, info.position.latitude],
        dataInfo: info.dataInfo
      })
```

---

```jsx
new BasePlottingInfo({
        id: info.id,
        name: info.name,
        position: [info.position.longitude, info.position.latitude],
        dataInfo: info.dataInfo
      })
```

---

```jsx
new BasePlottingInfo({
        id: info.id,
        name: info.name,
        position: [info.position.longitude, info.position.latitude],
        dataInfo: info.dataInfo
      })
```

---

```jsx
new BasePlottingInfo({
        id: info.id,
        name: info.name,
        position: [info.position.longitude, info.position.latitude],
        dataInfo: info.dataInfo
      })
```
